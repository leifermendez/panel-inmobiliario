import { BackgroundAccPipe } from './background-acc.pipe';

describe('BackgroundAccPipe', () => {
  it('create an instance', () => {
    const pipe = new BackgroundAccPipe();
    expect(pipe).toBeTruthy();
  });
});
