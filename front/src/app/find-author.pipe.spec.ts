import { FindAuthorPipe } from './find-author.pipe';

describe('FindAuthorPipe', () => {
  it('create an instance', () => {
    const pipe = new FindAuthorPipe();
    expect(pipe).toBeTruthy();
  });
});
